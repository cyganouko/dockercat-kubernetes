apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: dockercat
    component: backend
spec:
  type: ClusterIP
  selector:
    app: dockercat
    component: backend
  ports:
    - name: http
      port: 5000
      targetPort: 5000
