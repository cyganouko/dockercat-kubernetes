apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: dockercat
    component: frontend
spec:
  type: NodePort
  selector:
    app: dockercat
    component: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 80
      nodePort: 30080
